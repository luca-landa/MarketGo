use MarketGo

db.createUser({
    user: 'node-red',
    pwd: 'MarketGo',
    roles: ['readWrite']
})

db.createCollection("shelves")

db.shelves.insert([
      {idx: 0, quantity: 7, minimumQuantity: 2},
      {idx: 1, quantity: 7, minimumQuantity: 2},
      {idx: 2, quantity: 7, minimumQuantity: 2}
])